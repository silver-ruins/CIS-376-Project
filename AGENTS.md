# Project guidance for Dandelion Parade

- Keep the site lightweight and browser-based.
- Prefer semantic HTML, accessible labels, and simple JavaScript patterns.
- Preserve the current localStorage and sessionStorage-based workflow unless a backend is introduced.
- Keep UI updates consistent across the home, projects, auth, and admin pages.
- Use Bootstrap-friendly utility classes where helpful, but keep custom styling in assets/css/style.css.
